import 'package:flutter_test/flutter_test.dart';

void main() {
  // Full App widget smoke needs ProviderScope + window_manager/tray desktop bindings.
  test('placeholder smoke test', () {
    expect(2 + 2, 4);
  });
}
