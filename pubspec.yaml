name: pomodoro_sczuw
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# Synced from ./VERSION by scripts/app_version.sh (make sync-version / build / release).
# Edit VERSION, not this line. Format: <semver>+<build> e.g. 1.1.0+2
version: 1.3.0+5

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.41.0 <4.0.0'

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  tray_manager: ^0.5.1
  window_manager: ^0.5.1
  flutter_localizations:
    sdk: flutter
  intl: any
  flutter_riverpod: ^2.6.1
  dio: ^5.9.0
  flutter_local_notifications: ^19.5.0
  audioplayers: ^6.5.1
  hive_flutter: ^1.1.0
  hive: ^2.2.3
  package_info_plus: ^10.2.1
  file_selector: ^1.1.0
  path: ^1.9.1
  flutter_foreground_task: ^10.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  hive_generator: ^2.0.1
  build_runner: ^2.4.13
  flutter_launcher_icons: ^0.14.4

flutter_launcher_icons:
  android: true
  ios: false
  image_path: assets/images/pomodoro_launcher_icon.png
  adaptive_icon_background: "#000000"
  adaptive_icon_foreground: assets/images/pomodoro_launcher_icon.png
  min_sdk_android: 21

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true
  generate: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/
    - assets/images/
    - assets/sounds/
    - assets/images/pomodoro_app_icon_red.png
    - assets/images/pomodoro_app_icon_green.png
    - assets/images/pomodoro_app_icon_yellow.png
    - assets/images/pomodoro_app_icon_purple.png
    - assets/images/pomodoro_app_icon_gray.png
    - assets/images/pomodoro_app_icon_blue.png
    - assets/images/pomodoro_app_icon_red.ico
    - assets/images/pomodoro_app_icon_green.ico
    - assets/images/pomodoro_app_icon_yellow.ico
    - assets/images/pomodoro_app_icon_purple.ico
    - assets/images/pomodoro_app_icon_gray.ico
    - assets/images/pomodoro_app_icon_blue.ico
    - assets/sounds/request.mp3
    - assets/sounds/toggle.mp3



  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
